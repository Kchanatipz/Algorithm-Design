#include <iostream>
#include <vector>

using namespace std;
int n, m, k;
vector<int> v;

int recur(int start) {
    return 0;
}

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);
    cin >> n >> m >> k;
    v.resize(n);
    for (int i = 0; i < n; i++) {
        cin >> v[i];
    }
    while (n--) {
    }
    return 0;
}